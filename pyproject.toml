[project]
name = "quickstart-playwright-python"
version = "0.1.0"
description = "Playwright quickstart with Browserbase"
requires-python = ">=3.10"
dependencies = [
    "browserbase>=1.6.0",
    "playwright>=1.58.0",
    "python-dotenv>=1.2.2",
]
